'use strict';

describe('myApp.artistinfo module', function() {

  beforeEach(module('myApp.artistinfo'));

  describe('artistinfo controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var artistinfoCtrl = $controller('View1Ctrl');
      expect(artistinfoCtrl).toBeDefined();
    }));

  });
});